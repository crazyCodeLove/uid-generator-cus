server:
  port: 8061

spring:
  application:
    name: uid-generator
  datasource:
    hikari:
      jdbc-url: jdbc:mysql://127.0.0.1:3306/snowflakecus
      username: root
      password: root123
      minimum-idle: 3
      connection-timeout: 30000
      maximum-pool-size: 50
      idle-timeout: 60000
      driver-class-name: com.mysql.jdbc.Driver


mybatis:
  mapper-locations: classpath*:mapper/*.xml

uid:
  generate:
    timestampBits: 41
    workIdBits: 5
    sequenceBits: 17
    epochStr: 2018-12-21

  workNode:
    nodeId: 0



